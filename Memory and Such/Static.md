This stores:
1. The programs binary
2. Static variables (constants)

Properties:
	-> Fixed size 
	-> Everything is known from compile time (value && size)
	-> Lifetimes: until the program exits
	-> As a programmer you typically don't manage it